import express from 'express';
// import questionController from '../controllers/questionController.js';

const questionRoutes = express.Router();

// Define routes for all services used in the controller
// questionRoutes.get("", questionController.getAllQuestions);
// questionRoutes.post("", questionController.createQuestion);

// questionRoutes.get("/:id", questionController.findQuestionById);

// questionRoutes.put("/update/:questionId", questionController.updateQuestion);
// questionRoutes.delete("/delete/:questionId", questionController.deleteQuestion);


// questionRoutes.get("/evaluation/:evaluationId", questionController.getQuestionsByEvaluationId);

export default questionRoutes;

