import express from 'express';
// import choiceController from '../controllers/choiceController.js';

const choiceRoutes = express.Router();

//Requires nothing
// choiceRoutes.get("", choiceController.getAllChoices);

// choiceRoutes.get("/question/:questionId", choiceController.getChoicesByQuestionId);

// choiceRoutes.post("", choiceController.createChoice);

// choiceRoutes.put("/update/:choiceId", choiceController.updateChoice);

// choiceRoutes.delete("/delete/:choiceId", choiceController.deleteChoice);

export default choiceRoutes;